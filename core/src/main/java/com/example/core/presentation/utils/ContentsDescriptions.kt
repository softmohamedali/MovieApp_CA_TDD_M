package com.example.core.presentation.utils

//this object is very useful for many reason but the
//main reason is to allow to ui testing in compose where we use those constants
// to identify compose ui component to assert there visual value or any value


object ContentsDescriptions {
    const val PROGRESS_BAR="loading Circular progress"
}